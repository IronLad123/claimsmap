{
  "name": "claimsmap",
  "version": "1.0.0",
  "private": true,
  "description": "ClaimsMap — Cross-Document Fact Verification & Reconciliation Layer",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend start"
  }
}
